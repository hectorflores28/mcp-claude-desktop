# ============================================================
# NOTA HISTÓRICA
# Este proyecto fue construido cuando los MCPs (Model Context Protocol)
# no tenían integración nativa con Claude Desktop. Simulaba un servidor
# MCP mediante una API REST con FastAPI + Redis.
# Hoy (2025+) esto está superseded por el SDK oficial de MCP de Anthropic.
# ============================================================

# API y Servidor Web
fastapi==0.110.0
uvicorn==0.27.1
pydantic==2.6.4
pydantic-settings==2.2.1
python-dotenv==1.0.1

# Caché (Redis — reemplazable por in-memory en implementaciones modernas)
redis==5.0.2
aioredis==2.0.1

# Cliente HTTP y archivos
httpx==0.27.0
aiofiles==24.1.0
python-multipart==0.0.9

# Seguridad / Auth (JWT manual — hoy innecesario en MCPs locales)
python-jose==3.3.0
passlib==1.7.4
bcrypt==4.1.2

# Claude API
anthropic==0.19.1
backoff==2.2.1

# Monitoreo
prometheus-client==0.20.0
python-json-logger==2.0.7

# Utilidades
anyio==4.3.0
sniffio==1.3.1
typing-extensions==4.10.0

# Testing
pytest==8.0.2
pytest-asyncio==0.23.5.post1
pytest-cov==4.1.0
pytest-mock==3.12.0
pytest-env==1.1.3
coverage==7.4.3

# Desarrollo
black==24.2.0
isort==5.13.2
flake8==7.0.0
